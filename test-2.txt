        string(
            name: 'CONNECTOR_NAME',
            defaultValue: '',  // Default empty
            description: 'Enter the name of the connector(s) you wish to update:\n- Leave blank if updating all connectors.\n- For multiple connectors, use comma-separated values (e.g., "connector-1, connector-2").'
        )
