<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<title>Sequential Certificate Entry</title>
<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.3/dist/css/bootstrap.min.css">
<style>
    .input-group {
        display: flex; /* Enables flexbox */
        justify-content: space-between; /* Distributes space between and around content items */
        margin-bottom: 10px;
        flex-wrap: wrap; /* Allows items to wrap as needed */
    }
    .input-group > label {
        margin-right: 10px; /* Adds some space between the label and the input */
        flex: 1; /* Flex grow factor */
    }
    .input-group > input {
        flex: 2; /* Flex grow factor */
    }
    .container {
        max-width: 800px; /* Limits the width to prevent overly wide inputs */
    }
</style>
</head>
<body>
<div class="container mt-4">
    <h2>Enter Environment Certificates</h2>
    <form id="certificateForm">
        <div id="inputContainer">
            <!-- Dynamic input fields will be added here -->
        </div>
        <button type="button" id="addCertButton" class="btn btn-primary mt-3" onclick="addCertificateField()">Add Certificate</button>
        <button type="submit" class="btn btn-success mt-3">Submit All Certificates</button>
    </form>
</div>

<script>
let currentCertIndex = 0;
const certLabels = ['Dev Cert', 'QA Cert', 'UAT Cert', 'Prod Cert']; // Labels for the certificates

function addCertificateField() {
    if(currentCertIndex >= certLabels.length) {
        document.getElementById('addCertButton').disabled = true; // Disable button after all fields are added
        return;
    }

    const container = document.getElementById('inputContainer');
    const inputGroup = document.createElement('div');
    inputGroup.className = 'input-group';

    const label = document.createElement('label');
    label.textContent = certLabels[currentCertIndex] + ':';
    label.htmlFor = 'cert' + currentCertIndex;

    const input = document.createElement('input');
    input.type = 'text';
    input.className = 'form-control';
    input.id = 'cert' + currentCertIndex;
    input.placeholder = 'Enter ' + certLabels[currentCertIndex] + ' Details';

    inputGroup.appendChild(label);
    inputGroup.appendChild(input);
    container.appendChild(inputGroup);

    currentCertIndex++;
}

</script>
</body>
</html>
