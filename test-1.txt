management:
  endpoint:
    env:
      show-values: ALWAYS
      keys-to-sanitize:
        - ".*password.*"
        - ".*secret.*"
        - ".*key.*"
        - ".*token.*"
        - ".*credentials.*"
        - "dbPassword"
        - "keyStorePassword"
